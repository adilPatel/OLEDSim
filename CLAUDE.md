When using PyTorch functions, add a short inline comment of why you're using it.
Double-check whether detach() calls are necessary.
When documenting the code, only put comments that are describing the code as-written, rather than referencing previous runs. Focus on what it does and write the equation you're implementing, if applicable.
When you're implementing a model in code, add short inline comments of how the code implements it. The user should be able to walk through it without excessively verbose comments.